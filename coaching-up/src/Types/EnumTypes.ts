// TODO: Separate these into separate enums
enum ListingTags {
    // Methods
    Online = 'Online',
    InPerson = 'In-Person',
    Hybrid = 'Hybrid',
}

enum AccountType {
    Coach = 'Coach',
    Client = 'Client',
}

enum TimeIntervals {
    Session = 'Session',
    Month = 'Month',
    Year = 'Year',
}

export { ListingTags, TimeIntervals, AccountType };